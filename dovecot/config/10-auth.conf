# Allow plaintext auth (local use only — no TLS required on loopback)
disable_plaintext_auth = no
auth_mechanisms = plain login

passdb {
  driver = static
  args = password=voiceinbox
}

userdb {
  driver = static
  args = uid=vmail gid=vmail home=/var/mail/voiceinbox
}
